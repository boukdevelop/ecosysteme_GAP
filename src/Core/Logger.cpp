#include "Core/Logger.h"

namespace Ecosystem {
    namespace Core {
        // Par défaut, verbose activé (utile en développement). Peut être désactivé dans `main`.
        bool gVerbose = true;
    }
}
